# Flat-File-Story-Reader
Single-file PHP application that reads Markdown stories from disk and renders them in a clean, episode-based UI. Includes front-matter support, JSON endpoint, and client-side Markdown rendering. No database or framework required.
